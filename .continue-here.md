---
phase: 08-polish-ux (+ ad-hoc bug fixes)
status: paused
last_updated: 2026-03-28T08:21:11Z
---

<current_state>
Ad-hoc bug fix session. Two gameplay bugs fixed (uncommitted). Phase 08 plan 04 (audio) still unstarted. Juggernaut jump behavior needs rebuild + verification.
</current_state>

<completed_work>

- Port placement in coastal corners — `hasAdjacentLand()` now checks 8 directions (diagonal land counts as coastal). File: `GameInputController.java`
- Juggernaut jump arc raised 50f → 120f. File: `CombatConstants.java`
- `MovementComponent.jumpArcHeight` field added; `UnitRenderSystem` applies sin arc during movement for any unit with it set. Files: `MovementComponent.java`, `UnitRenderSystem.java`
- (Previous sessions) Phase 08 tech debt tasks 1–10 all committed.
</completed_work>

<remaining_work>

- **Verify juggernaut jump attack** — user saw normal range damage instead of instant-kill+AoE. Likely stale binary. Rebuild and check logs for `[ABILITY] Juggernaut JUMP`. If still broken, add log to `performJump()` to trace the call path.
- **08-04-PLAN.md** — audio gaps (defend SFX, BGM wiring, fallback files). Not started.
- **Tech debt #1** — tests for CombatSystem, UnitFactory, GameInputController. Not started.
</remaining_work>

<decisions_made>

- `hasAdjacentLand` is 8-directional because corner water tiles are diagonally adjacent to land (not cardinally). Cardinal-only check was the bug.
- Juggernaut NEVER uses `MovementComponent` — `showRangeMarkers` skips blue markers for it entirely. Its visual movement is `AnimationComponent.JUMP` via `resolveJumperAttack`. The `MovementComponent.jumpArcHeight` field applies to other units only.
- Arc raised to 120f (was 50f) — 50f was visually subtle given tile scale.
</decisions_made>

<blockers>
None hard-blocking. Juggernaut needs rebuild + retest.
</blockers>

<context>
The juggernaut's attack and movement are the same action: all tiles within range 4 get red attack markers (not blue movement markers). Clicking any marker calls `performJump` → `resolveJumperAttack` → grid teleport + `AnimationComponent.JUMP` arc + deferred `JumpLandingComponent` landing (instant kill + AoE after 0.7s). The landing fires in `CombatSystem.update()` once `AnimationSystem` sets `jlc.landed = true`.

Pre-existing uncommitted files (not from this session): `StructureData.java`, `SaveManager.java`, `GameScreen.java`, `StructureEconomySystem.java`, `LevelUpPopup.java`.
</context>

<next_action>
1. `./gradlew run` in PowerShell to rebuild
2. Test juggernaut jump attack — check logs for `[ABILITY] Juggernaut JUMP`
3. If working: commit this session's changes, then tackle 08-04-PLAN.md audio
4. If still broken: add `GameLogger.log(...)` inside `performJump()` to trace the path
</next_action>
