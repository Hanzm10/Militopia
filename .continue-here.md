---
phase: tech-debt (ad-hoc)
task: Phase B not started
status: paused
last_updated: 2026-03-27T13:58:05.469Z
---

<current_state>
Phase A of the tech debt refactor is complete and committed (ee6c4bf). Ready to start Phase B: replacing all string-based unit type checks with a UnitType enum.
</current_state>

<completed_work>

- Phase A committed at ee6c4bf: `refactor: Phase A tech debt cleanup`
  - Created `core/src/main/java/com/militopia/config/CombatConstants.java` — 16 named constants extracted from CombatSystem
  - Replaced all magic number literals in `CombatSystem.java` with CombatConstants references
  - Replaced 4x `Gdx.app.log` and 1x `System.out.println` with `GameLogger.logScreen()` in GameScreen.java and LoadGameScreen.java
  - Removed duplicate `gdx-freetype` dependency in `core/build.gradle`
  - Build compiles clean; 3 pre-existing test failures in AbilityTest (entityFactory null NPE, not caused by our changes)
</completed_work>

<remaining_work>

Phase B — UnitType enum migration:
- [ ] Create `com.militopia.data.UnitType` enum with 16 values: RECRUIT, RANGER, SNIPER, TANK, JUGGERNAUT, RECON_DRONE, SUICIDE_DRONE, APACHE, B2, GUNBOAT, DESTROYER, CARRIER, SUBMARINE, TITAN, WRAITH, DREADNOUGHT
- [ ] Add `unitType` field to `StatsComponent.java` alongside existing `unitTypeKey` String (dual-field approach — keep both during migration)
- [ ] Migrate 21 string comparisons in `CombatSystem.java`
- [ ] Migrate 21 switch cases in `UnitFactory.java` (1291 lines — read carefully)
- [ ] Migrate 3 comparisons in `GameInputController.java`
- [ ] Migrate 3 comparisons in `InfoPanel.java`
- [ ] Migrate 3 comparisons in `AbilityStatusSystem.java`
- [ ] Migrate 1 comparison in `UnitRenderSystem.java`
- [ ] Update `SaveManager.java` serialization: write `unitType.name()`, read via `UnitType.valueOf()`
- [ ] Remove `unitTypeKey` field from `StatsComponent` once all 44 comparisons are gone
</remaining_work>

<decisions_made>

- Use dual-field migration: add `unitType` enum field to StatsComponent while keeping `unitTypeKey` String. Migrate files one at a time, remove `unitTypeKey` last. This keeps the build green throughout.
- `CombatConstants` lives in `com.militopia.config` (same package as `GameConfig`)
- The 3 failing AbilityTest tests are pre-existing failures — not introduced by our changes
</decisions_made>

<blockers>
- Gradle build cannot be run from Claude's terminal in this environment (loopback connection error). User must run `./gradlew build` manually from PowerShell to verify each step.
</blockers>

<context>
This is an ad-hoc tech debt refactor — not part of any formal GSD phase. It was kicked off from a fresh tech debt audit this session. The full prioritized report with scores is in the conversation history.

Phase B is token-heavy because UnitFactory.java is 1291 lines. Start a fresh session to keep context clean. The migration order matters: read StatsComponent and SaveManager first to understand the data model, then create the enum, then migrate file by file.
</context>

<next_action>
Start with: "continuing Phase B of the tech debt refactor — UnitType enum. Phase A is done (commit ee6c4bf). Read StatsComponent.java and SaveManager.java first, then create UnitType enum, then add unitType field to StatsComponent using dual-field migration approach."
</next_action>
