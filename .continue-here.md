---
phase: ad-hoc feature additions (on top of 08-polish-ux)
status: paused
last_updated: 2026-03-28T11:32:15Z
---

<current_state>
Ad-hoc gameplay feature session. Multiple new mechanics added and committed (see dc0c8a3).
One uncommitted change: CombatConstants.java (ANIMAL_HUNT_FUNDING manually changed 1→3 by user).
A display-as-map-sprite plan was written but NOT implemented (user rejected ExitPlanMode).
</current_state>

<completed_work>

**This session (ad-hoc features):**
- INVINCIBLE_TINT_ALPHA = 0.5f in CombatConstants; UnitRenderSystem uses it (was hardcoded)
- TREE_CUT_FUNDING = 1, ANIMAL_HUNT_FUNDING = 3 in CombatConstants (user set hunt to 3)
- TERRAIN_BONUS_BASE = 2 in CombatConstants; CombatSystem.terrainDefBonus handles all BASE_* tiles
- Cut Tree action: InfoPanel button for unit on TREE tile — removes entity + map object, awards funding, exhausts unit
- Disband action: InfoPanel button for own units — half cost refund, removes entity
- Demolish structure refund: Demolish now grants cost/2 funding and shows amount in label
- Fog double-layer: MapRenderSystem draws fog at both original Y and lifted Y (+5px)
- Tree visual fix: Cut Tree removes the ECS entity so sprite disappears immediately
- Animal hunt reward configurable: GameInputController uses CombatConstants.ANIMAL_HUNT_FUNDING

**Previous session (bug fixes, committed):**
- Port coastal corner fix (hasAdjacentLand 8-directional)
- Juggernaut jump arc 50f -> 120f
- Phase 08 tech debt tasks 1-10 committed
</completed_work>

<remaining_work>

- **Uncommitted**: CombatConstants.java — ANIMAL_HUNT_FUNDING=3 (commit this first)
- **Display-as-map-sprite** (plan written, NOT implemented):
  Plan: C:\Users\Hanz Mapua\.claude\plans\swirling-dancing-ladybug.md
  Files to change: GameConfig.java, StatsComponent.java, UnitFactory.java, UnitRenderSystem.java
  User rejected ExitPlanMode — discuss before implementing
- **08-04-PLAN.md** — audio gaps (defend SFX, BGM wiring). Not started.
- **Juggernaut jump** — still needs rebuild + retest from previous session
</remaining_work>

<decisions_made>

- Cut Tree marks unit hasActed+hasMoved (full action, not free)
- Disband available regardless of hasActed (like structure demolish)
- Base defense bonus uses obj.name().startsWith("BASE_") to cover all level variants
- Display-as-map plan: auto-flip display texture for left facing; new GameConfig constants for draw size/offset
</decisions_made>

<blockers>
None hard-blocking.
</blockers>

<context>
All new features use CombatConstants for configurability. Display-as-map feature would let new
units be registered with a single display image instead of 3 separate assets (_right, _left, _display).
Plan is ready at C:\Users\Hanz Mapua\.claude\plans\swirling-dancing-ladybug.md — needs user go-ahead.
</context>

<next_action>
1. Commit CombatConstants.java (ANIMAL_HUNT_FUNDING=3)
2. Ask user: proceed with display-as-map-sprite plan?
3. Then: 08-04-PLAN.md audio / juggernaut retest
</next_action>
