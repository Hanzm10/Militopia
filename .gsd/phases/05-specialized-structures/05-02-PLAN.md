---
phase: 05-specialized-structures
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - core/src/main/java/com/militopia/ui/GameHUD.java
  - core/src/main/java/com/militopia/controller/GameInputController.java
  - core/src/main/java/com/militopia/factories/UnitFactory.java
autonomous: false
requirements: [STR-03, STR-04]
must_haves:
  truths:
    - "Ports only display Sea units in their summon menu"
    - "Sea units spawn directly on top of the Port"
    - "Bases no longer display Sea units"
  artifacts:
    - path: "core/src/main/java/com/militopia/ui/GameHUD.java"
      provides: "Filtered unit list based on producer type"
  key_links:
    - from: "GameInputController"
      to: "GameHUD"
      via: "openSummonMenu(owner, state, level, producerType)"
---

<objective>
Implement the specialized Port behavior, including Sea-unit-only filtering and localized spawning.

Purpose: Finalizes the Port as the exclusive production hub for naval forces.
Output: Filtered HUD menus and Port-specific interaction logic.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Filtered Summon Menus in GameHUD</name>
  <files>core/src/main/java/com/militopia/ui/GameHUD.java, core/src/main/java/com/militopia/factories/UnitFactory.java</files>
  <action>
    Update GameHUD.openSummonMenu to accept a 'producerType' (e.g., BASE, PORT).
    Update populateSummonMenu to filter units:
    - BASE: Exclude Sea MoveType (Gunboat, Destroyer, Carrier).
    - PORT: Include ONLY Sea MoveType.
    Ensure Port correctly respects the Level 2 unlock requirement in the Build Menu.
  </action>
  <verify>Open Base menu -> confirm no Sea units. Open Port menu -> confirm only Sea units.</verify>
  <done>Summon menus are correctly filtered by producer type.</done>
</task>

<task type="auto">
  <name>Task 2: Port Interaction and Naval Spawning</name>
  <files>core/src/main/java/com/militopia/controller/GameInputController.java, core/src/main/java/com/militopia/factories/UnitFactory.java</files>
  <action>
    Update handleStructureTarget in GameInputController to recognize an owned PORT.
    Trigger GameHUD.openSummonMenu with 'PORT' producer type.
    Ensure UnitFactory.createUnit correctly handles spawning at the Port coordinates (already likely, but verify).
  </action>
  <verify>Click an owned Port -> verify 'SEA' unit menu appears. Summon Gunboat -> verify it spawns on the Port.</verify>
  <done>Port-specific interaction logic is implemented and verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Port interaction and Naval Spawning</what-built>
  <how-to-verify>
    1. Reach Level 2.
    2. Build a Port on a coastal water tile.
    3. Click the Port.
    4. Select 'Gunboat'.
    5. Verify Gunboat spawns directly on the Port's tile.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<success_criteria>
- Sea units are exclusive to Ports.
- Naval units spawn correctly at the Port location.
- Port correctly respects the Level 2 unlock requirement.
</success_criteria>
