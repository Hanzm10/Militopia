---
wave: 3
depends_on: [08-02-PLAN.md]
files_modified:
  - core/src/main/java/com/militopia/managers/TurnHistoryManager.java
  - core/src/main/java/com/militopia/controller/GameInputController.java
  - core/src/main/java/com/militopia/ui/GameHUD.java
autonomous: true
requirements: []
---

# Wave 3: Undo Mechanics

## Objective
Enhance snapshot granularity to snapshot before discrete actions, build the visual undo log in a Right Panel, and wire up `Ctrl+Z` logic to rollback GameState.

<task>
  <objective>Action Snapshot Granularity</objective>
  <read_first>
    - core/src/main/java/com/militopia/managers/TurnHistoryManager.java
    - core/src/main/java/com/militopia/controller/GameInputController.java
  </read_first>
  <action>
    In `GameInputController.java`, invoke `turnHistoryManager.push(state.snapshot())` IMMEDIATELY BEFORE committing movement, building, attack, or any state-altering logic.
    Ensure `TurnHistoryManager` gracefully handles this higher frequency, retaining its 10 snapshot limits.
  </action>
  <acceptance_criteria>
    - GameInputController.java calls `push()` before actions, not just at turn execution boundaries.
  </acceptance_criteria>
</task>

<task>
  <objective>Ctrl+Z Hotkey Listener</objective>
  <read_first>
    - core/src/main/java/com/militopia/controller/GameInputController.java
  </read_first>
  <action>
    In `GameInputController.java`, capture `Input.Keys.Z` while `Keys.CONTROL_LEFT` or `Keys.CONTROL_RIGHT` is pressed.
    On firing, extract the previous snapshot from `TurnHistoryManager.undo()`.
    Restore the global `GameState` map objects to mirror the snapshot contents, completely resetting positions and HPs.
    After restoring state, immediately call GameHUD's update methods so the visual top bar (XP, funding, income) resets to the undone snapshot values.
  </action>
  <acceptance_criteria>
    - GameInputController lists `keyDown(int keycode)` logic detecting Ctrl+Z combination.
    - Restores Ashley engine entities reflecting snapshot data cleanly.
  </acceptance_criteria>
</task>

<task>
  <objective>Right Panel Snapshot Overlay</objective>
  <read_first>
    - core/src/main/java/com/militopia/ui/GameHUD.java
  </read_first>
  <action>
    Add a collapsible `Table` panel mounted to the right side of the screen in `GameHUD.java`.
    This table reads directly from `TurnHistoryManager` history bounds, rendering 1-10 labels describing "Action {N}". 
    Add a manual "Undo" button near the settings gear icon to replicate the hotkey.
  </action>
  <acceptance_criteria>
    - GameHUD adds a right-aligned Table for snapshot items.
    - Settings button cluster contains a dedicated Undo TextButton.
  </acceptance_criteria>
</task>

## Verification
- Pressing `Ctrl+Z` actually rewinds an entity position or attack outcome exactly 1 step backward.
- The right panel shrinks back down if the stack pops below 10.
- `Undo` via on-screen button matches hotkey logic.

## Must Haves
- Undo cleanly syncs both `GameState` data and the Ashley Engine entities back to snapshot definitions without leaking orphaned entities on the screen.
