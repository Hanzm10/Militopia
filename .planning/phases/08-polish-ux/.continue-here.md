---
phase: 08-polish-ux
task: super-unit-system
total_tasks: 1
status: completed
last_updated: 2026-03-27T07:53:56.469Z
---

<current_state>
Super unit choice system fully implemented and compiles clean. 3 pre-existing AbilityTest failures (unrelated). Phase 08 plan 04 (audio gaps) still unexecuted — that was the last remaining plan before this session added the super unit feature as a new unplanned task.
</current_state>

<completed_work>

- Super unit system (unplanned, user-requested mid-session):
  - `StatsComponent` — added `chosenSuperUnit = null` field
  - `BaseLevelConfig` — removed `"SUPER_UNIT"` from level 5/6+ unlock lists
  - `StructureSnapshot` — added `chosenSuperUnit` to undo snapshot (constructor updated)
  - `UnitFactory` — added TITAN/WRAITH/DREADNOUGHT stats + `chooseSuperUnit()` + popup trigger in `checkAndApplyLevelUp()` at level >= 5
  - `SuperUnitChoicePopup.java` — NEW file, modal popup (no dismiss), 3 choice cards
  - `GameHUD` — added `superUnitChoicePopup` field + `showSuperUnitChoicePopup()` method
  - `SlideMenu.populateSummonContent()` — looks up base entity at clicked pos, adds chosen super unit as ★ button
  - `GameScreen.restoreSnapshot()` — restores `chosenSuperUnit` on undo

- All changes compile clean (35 tests, 3 pre-existing AbilityTest failures unrelated to this work)
</completed_work>

<remaining_work>

- Phase 08 plan 04 (08-04-PLAN.md) still unexecuted:
  - Wire defend/counter SFX in CombatSystem counterattack branch
  - Wire BGM playback on GameScreen show
  - Add fallback SFX asset files (attack_ranged.wav, attack_melee.wav, hit.wav, battle_theme.wav)
- Commit the super unit changes (not yet committed)
</remaining_work>

<decisions_made>

- Super units: TITAN (LAND, hp=60, atk=20, def=10), WRAITH (AIR, hp=30, atk=25), DREADNOUGHT (SEA, hp=70, atk=20, def=8) — all cost 25 to resummon
- Texture reuse: TITAN→JUGGERNAUT, WRAITH→B2, DREADNOUGHT→SUBMARINE (until custom assets added)
- Initial spawn is free (immediate on choice); re-summon from base costs 25 funding
- `chosenSuperUnit` stored on base's `StatsComponent`, persisted through undo via `StructureSnapshot`
- SlideMenu looks up base entity via `unitFactory.getEntityAt(bx, by, 1)` at last clicked position — no signature changes needed
- `GameHUD.showSuperUnitChoicePopup()` gets map from `screen.getGameMap()` — no extra params needed
</decisions_made>

<blockers>
- None
</blockers>

<context>
User asked to change the super unit selection rule: instead of choosing from a dropdown at level 5+, a popup (like the level-up popup) appears when the base reaches level 5, forcing the player to pick one of 3 super units. The chosen unit spawns immediately and is permanently available in the summon menu. The implementation follows the existing LevelUpPopup pattern exactly.
</context>

<next_action>
Commit the super unit changes first:
  git add core/src/main/java/com/militopia/components/StatsComponent.java
  git add core/src/main/java/com/militopia/config/BaseLevelConfig.java
  git add core/src/main/java/com/militopia/data/StructureSnapshot.java
  git add core/src/main/java/com/militopia/factories/UnitFactory.java
  git add core/src/main/java/com/militopia/ui/SuperUnitChoicePopup.java
  git add core/src/main/java/com/militopia/ui/GameHUD.java
  git add core/src/main/java/com/militopia/ui/SlideMenu.java
  git add core/src/main/java/com/militopia/screen/GameScreen.java

Then execute 08-04 (audio gaps): /gsd:execute-phase 08
</next_action>
