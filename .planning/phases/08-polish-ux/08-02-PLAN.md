---
wave: 2
depends_on: [08-01-PLAN.md]
files_modified:
  - core/src/main/java/com/militopia/managers/AudioManager.java
  - core/src/main/java/com/militopia/systems/RenderSystem.java
  - core/src/main/java/com/militopia/components/AnimationComponent.java
  - core/src/main/java/com/militopia/systems/CombatSystem.java
autonomous: true
requirements: []
---

# Wave 2: Animation & Audio

## Objective
Establish the `AudioManager` for SFX/BGM delivery, hook up logic events to audio cues, build the `AnimationComponent` for simple sprite loop/play states, and make melee attackers step into defender tiles on lethal hits.

<task>
  <objective>Establish AudioManager</objective>
  <read_first>
    - core/src/main/java/com/militopia/managers/AssetManager.java
    - .planning/phases/08-polish-ux/08-RESEARCH.md
  </read_first>
  <action>
    Create `AudioManager.java` functioning as a cache and player. 
    Wrap `com.badlogic.gdx.audio.Sound`. Must contain a rate-limiting array or threshold logic to prevent > 5 simultaneous copies of the exact same Sound file from playing in a single frame. Add `playBGM(String)` utilizing `Music`.
  </action>
  <acceptance_criteria>
    - core/src/main/java/com/militopia/managers/AudioManager.java exists.
    - Contains logic preventing concurrent Sound overlaps beyond a threshold.
  </acceptance_criteria>
</task>

<task>
  <objective>Introduce AnimationComponent & RenderSystem Processing</objective>
  <read_first>
    - core/src/main/java/com/militopia/systems/RenderSystem.java
  </read_first>
  <action>
    Create `AnimationComponent.java` holding `float stateTime`, `boolean loop`, and an `enum State { IDLE, MOVE, ATTACK }`.
    Modify `RenderSystem.java` to apply LibGDX `Animation` mapping. Update `CombatSystem` and movement logic to toggle these states respectively, along with camera `shake` adjustments.
  </action>
  <acceptance_criteria>
    - AnimationComponent.java exists.
    - RenderSystem.java contains stateTime += deltaTime logic processing the animations.
  </acceptance_criteria>
</task>

<task>
  <objective>Melee Advance Logic</objective>
  <read_first>
    - core/src/main/java/com/militopia/systems/CombatSystem.java
    - core/src/main/java/com/militopia/components/StatsComponent.java
  </read_first>
  <action>
    In `CombatSystem.java`'s lethal strike resolution: 
    If attacker's range is exactly 1 (Melee), update their `GridPositionComponent` to the defender's exact coordinates right after the defender is removed. Play movement SFX via `AudioManager`.
  </action>
  <acceptance_criteria>
    - CombatSystem contains a check for range == 1.
    - CombatSystem overwrites attacker position to defender position on death.
  </acceptance_criteria>
</task>

## Verification
- Melee units take the square of defeated units automatically.
- AudioManager enforces identical sound spam capping.
- Screen shakes appropriately during attacks.

## Must Haves
- Game doesn't freeze when calculating 10 attacks quickly in succession, SFX capping works properly.
