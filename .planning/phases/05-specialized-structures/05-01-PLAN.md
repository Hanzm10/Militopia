---
phase: 05-specialized-structures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/src/main/java/com/militopia/components/StatsComponent.java
  - core/src/main/java/com/militopia/factories/UnitFactory.java
  - core/src/main/java/com/militopia/controller/GameInputController.java
autonomous: true
requirements: [STR-01, STR-02]
must_haves:
  truths:
    - "Structures are linked to a parent Base for XP/Income distribution via StatsComponent"
    - "Placement validation prevents structures from being placed in invalid terrain or outside territory"
  artifacts:
    - path: "core/src/main/java/com/militopia/factories/UnitFactory.java"
      provides: "Structure-to-Base linking logic"
    - path: "core/src/main/java/com/militopia/controller/GameInputController.java"
      provides: "Land/Water and Radius validation"
  key_links:
    - from: "UnitFactory"
      to: "StatsComponent"
      via: "parentBaseX/Y fields"
---

<objective>
Implement the foundational structure-to-base linking system and placement validation logic.

Purpose: Ensures specialized structures contribute to the player's economy and XP growth correctly.
Output: Validated placement system and parent-child entity relationships.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement Parent-Base Linking in UnitFactory</name>
  <files>core/src/main/java/com/militopia/components/StatsComponent.java, core/src/main/java/com/militopia/factories/UnitFactory.java</files>
  <action>
    Modify StatsComponent to hold 'parentBaseX' and 'parentBaseY' (already exists in code but needs consistent usage).
    Update UnitFactory.createStructure() to ensure the link is established for ALL specialized structures. 
    Verify that XP/Income generated by these structures can be traced back to these coordinates.
  </action>
  <verify>Check if stats.parentBaseX/Y are correctly set after calling createStructure().</verify>
  <done>Structures correctly reference their nearest parent base via coordinates.</done>
</task>

<task type="auto">
  <name>Task 2: Specialized Placement Validation</name>
  <files>core/src/main/java/com/militopia/controller/GameInputController.java</files>
  <action>
    Refine 'handleTerrainSelection' logic in GameInputController.
    - Port: Must be on Water (WATER/DEEP_WATER), and MUST have at least one adjacent Land tile.
    - Others: Must be on Land (GRASS/SAND/MOUNTAIN).
    - All: Must be within 'isTerritory' check (existing radius logic).
    Ensure the Build Menu only displays valid options for the selected tile.
  </action>
  <verify>Attempt to place a Port on land (should fail) and an Oil Derrick on water (should fail). Verify Port requires land adjacency.</verify>
  <done>Placement is strictly enforced based on terrain, territory, and adjacency constraints.</done>
</task>

</tasks>

<success_criteria>
- Structures successfully link to parent bases upon creation.
- Ports can only be placed on water tiles with land adjacency.
- Non-port structures are restricted to land tiles.
</success_criteria>
